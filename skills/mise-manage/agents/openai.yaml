interface:
  display_name: "Mise Manage"
  short_description: "Manage mise tools globally and per project: config, backends, lockfiles, env/tasks, updates, and supply-chain warnings."
  default_prompt: "Use $mise-manage to install or pin tools globally or per project, choose a backend, set up lockfiles, or diagnose a mise warning or failed resolution."
